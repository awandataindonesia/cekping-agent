# Agent Configuration
SERVER_ADDR=your-server-domain.com:50051
AGENT_TOKEN=your-agent-token-from-registration

# Agent Info (optional, will be auto-detected if not set)
AGENT_HOSTNAME=my-probe-name
AGENT_VERSION=1.0.0
